<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MAUI_Custom_Controls.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MAUI_Custom_Controls"
    xmlns:viewmodels="clr-namespace:MAUI_Custom_Controls.ViewModels"
    x:DataType="viewmodels:MenuViewModel"
    Shell.FlyoutBehavior="Flyout">

    <Shell.BindingContext>
        <viewmodels:MenuViewModel/>
    </Shell.BindingContext>

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="Scroll Animation"
        ContentTemplate="{DataTemplate local:ScrollAnimationPage}"
        Route="ScrollAnimationPage" />

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

    <!--<MenuItem Text="Logout" Clicked="MenuItem_Clicked"/>-->
    <MenuItem Text="Logout" Command="{Binding MenuItemClickedCommand}"/>

</Shell>
